apiVersion: v1
kind: Service
metadata:
  labels:
    app: simple-dotNet-service
  name: simple-dotNet-service

spec:
  selector:
    app: simple-dotNet-service
  ports:
  - port: 8080
    protocol: TCP
    targetPort: 8080
